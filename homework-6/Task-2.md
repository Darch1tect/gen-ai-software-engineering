# Task update

## What you'll build:

You will take the homework-6 project and add two major capabilities:
1. A new agent + configurable rule engine
2. A REST API gateway — wrap your file-based pipeline behind HTTP endpoints so transactions can be submitted and results retrieved via API calls.
3. A demo script — a single demo.sh that starts everything, submits test transactions, and displays results with zero manual steps.

## Submission:
Upload your complete project as a .zip archive or share a GitHub repository link.